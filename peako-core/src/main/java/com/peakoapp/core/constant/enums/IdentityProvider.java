package com.peakoapp.core.constant.enums;

/**
 * The {@code IdentityProvider} enum class specifies the supported identity providers that the user
 * can use to sign in their account.
 *
 * @version 0.1.0
 */
public enum IdentityProvider {
    LOCAL,
}
