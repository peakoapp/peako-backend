package com.peakoapp.core.jwt;

/**
 * The {@code TokenType} enum class represents all the valid JWT tokens that can be created and
 * parsed correctly.
 *
 * @version 0.1.0
 */
public enum TokenType {
    ACCESS
}
